import styled from "styled-components";

const Link = styled.span`
  color: #ff0f7b;
  text-decoration: underline;
  user-select: none;
  &:hover {
    cursor: pointer;
  }
`;

export const s = {
  Link,
};
